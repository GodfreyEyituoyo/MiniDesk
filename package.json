{
  "name": "minidesk",
  "version": "1.0.0",
  "description": "MiniDesk — Mac Mini M4 Workstation Bundles",
  "private": true,
  "scripts": {
    "dev": "netlify dev",
    "build": "echo 'Static site — no build step needed'"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.49.1",
    "resend": "^4.1.2"
  },
  "devDependencies": {
    "netlify-cli": "^18.1.0"
  }
}
